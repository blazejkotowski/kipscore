= simple_form_for(@tournament) do |f|
  = f.error_notification

  .form-inputs
    = f.input :user_id
    = f.input :name
    = f.input :start_date
    = f.input :active

  .form-actions
    = f.button :submit
