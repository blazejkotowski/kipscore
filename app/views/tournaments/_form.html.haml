= simple_form_for(@tournament) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :start_date

  .form-actions
    = f.button :submit, :class => 'btn btn-large btn-primary'
